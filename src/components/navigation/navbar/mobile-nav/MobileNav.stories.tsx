import MobileNav from './MobileNav';

const MobileNavStory = {
  component: MobileNav,
  title: 'Navigation/Navbar/MobileNav',
  tags: ['autodocs'],
  decorators: (Story: React.FC) => <Story />,
};

export default MobileNavStory;

export const Default = {
  args: {},
};
