import MobileNavItem from './MobileNavItem';

const MobileNavItemStory = {
  component: MobileNavItem,
  title: 'Navigation/Navbar/MobileNav/MobileNavItem',
  tags: ['autodocs'],
  decorators: (Story: React.FC) => <Story />,
};

export default MobileNavItemStory;

export const Default = {
  args: {},
};
